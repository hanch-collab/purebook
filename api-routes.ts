// ============================================================
// Purebook — API Routes
// All routes use Next.js App Router API conventions
// Base path: /api/
//
// Auth: NextAuth session checked via getServerSession()
// Errors: { error: string, code?: string } shape
// Success: { data: T } shape
// Pagination: { data: T[], meta: { total, page, limit } }
// ============================================================